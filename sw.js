const CACHE_NAME = 'masrofi-v1';// الملفات اللي لازم تتخزن لتشتغل بدون نتconst urlsToCache = [  './',  './index.html',  './manifest.json',  './icon.png'  // رح نضيف ملفات الـ CSS والـ JS هون بس نخلصهم];// تنصيب السيرفيس وركر وحفظ الملفاتself.addEventListener('install', event => {  self.skipWaiting();  event.waitUntil(    caches.open(CACHE_NAME).then(cache => cache.addAll(urlsToCache))  );});// تفعيل وتحديث الكاش إذا تغير شيself.addEventListener('activate', event => {  event.waitUntil(    caches.keys().then(cacheNames => {      return Promise.all(        cacheNames.map(cacheName => {          if (cacheName !== CACHE_NAME) {            return caches.delete(cacheName);          }        })      );    }).then(() => self.clients.claim())  );});// جلب الملفات من الكاش أولاً (لأجل الأوفلاين)self.addEventListener('fetch', event => {  event.respondWith(    caches.match(event.request).then(response => response || fetch(event.request))  );});